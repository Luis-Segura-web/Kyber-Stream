<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso para acceder a Internet, esencial para cargar streams IPTV -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- ¡NUEVO! Permiso para modificar la configuración de audio (control de volumen del dispositivo) -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KyberStream"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- MainActivity: La actividad principal que contendrá la navegación de la app -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.KyberStream"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity: La actividad para la autenticación y selección de perfil -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.KyberStream">
            <!-- No se necesita intent-filter MAIN aquí, ya que MainActivity es el punto de entrada principal después del login -->
        </activity>

        <!-- PlayerActivity: La actividad dedicada exclusivamente a la reproducción de video en pantalla completa -->
        <activity
            android:name=".ui.player.PlayerActivity"
            android:exported="false"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:theme="@style/Theme.KyberStream"
            tools:ignore="LockedOrientationActivity" />

    </application>
</manifest>
